export interface IngredientType {
  name: string;
  required: boolean;
  switchable: boolean;
  active: boolean;
}

export interface CsvIngredientType {
  nombre: string;
  requerido: string;
  intercambiable: string;
  activo: string;
}
